{
    "engines": {
        "node": ">=18"
    },
    "scripts": {
        "build": "composer install --no-dev --optimize-autoloader && php bin/console doctrine:schema:update --force",
        "start": "php -S 0.0.0.0:8000 -t public"
    }
}
