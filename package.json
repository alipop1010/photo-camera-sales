{
    "name": "photo-camera-sales",
    "version": "1.0.0",
    "description": "Symfony PHP application",
    "scripts": {
        "build": "curl -sS https://getcomposer.org/installer | php && php composer.phar install --no-dev --optimize-autoloader && php bin/console doctrine:schema:update --force",
        "start": "php -S 0.0.0.0:8000 -t public"
    },
    "engines": {
        "node": ">=18"
    }
}
