{% extends 'base.html.twig' %}

{% block title %}Главная - Система продажи фотоаппаратов{% endblock %}

{% block body %}
<div class="container-fluid mt-4">
    <!-- Заголовок -->
    <div class="row mb-5">
        <div class="col-12">
            <div class="text-center">
                <h1 class="display-4 fw-bold text-primary">📷 Система продажи фотоаппаратов</h1>
                <p class="lead text-muted">Управление брендами, камерами, покупателями и продажами</p>
            </div>
        </div>
    </div>

    <!-- Статистика -->
    <div class="row mb-5">
        <div class="col-md-3 mb-4">
            <div class="card border-primary shadow-sm h-100">
                <div class="card-body text-center">
                    <div class="mb-3">
                        <span class="display-4">🅱️</span>
                    </div>
                    <h3 class="card-title text-primary">{{ stats.brands }}</h3>
                    <p class="card-text">Брендов</p>
                    <a href="{{ path('app_brand') }}" class="btn btn-outline-primary btn-sm">Перейти</a>
                </div>
            </div>
        </div>
        
        <div class="col-md-3 mb-4">
            <div class="card border-success shadow-sm h-100">
                <div class="card-body text-center">
                    <div class="mb-3">
                        <span class="display-4">📸</span>
                    </div>
                    <h3 class="card-title text-success">{{ stats.cameras }}</h3>
                    <p class="card-text">Фотоаппаратов</p>
                    <a href="{{ path('app_camera') }}" class="btn btn-outline-success btn-sm">Перейти</a>
                </div>
            </div>
        </div>
        
        <div class="col-md-3 mb-4">
            <div class="card border-warning shadow-sm h-100">
                <div class="card-body text-center">
                    <div class="mb-3">
                        <span class="display-4">👥</span>
                    </div>
                    <h3 class="card-title text-warning">{{ stats.customers }}</h3>
                    <p class="card-text">Покупателей</p>
                    <a href="{{ path('app_customer') }}" class="btn btn-outline-warning btn-sm">Перейти</a>
                </div>
            </div>
        </div>
        
        <div class="col-md-3 mb-4">
            <div class="card border-info shadow-sm h-100">
                <div class="card-body text-center">
                    <div class="mb-3">
                        <span class="display-4">🛒</span>
                    </div>
                    <h3 class="card-title text-info">{{ stats.purchases }}</h3>
                    <p class="card-text">Продаж</p>
                    <a href="{{ path('app_purchase') }}" class="btn btn-outline-info btn-sm">Перейти</a>
                </div>
            </div>
        </div>
    </div>

    <!-- Последние продажи и быстрые ссылки -->
    <div class="row">
        <!-- Последние продажи -->
        <div class="col-lg-8 mb-4">
            <div class="card shadow-sm">
                <div class="card-header bg-light">
                    <h5 class="mb-0">🔄 Последние продажи</h5>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead class="table-dark">
                                <tr>
                                    <th width="80">ID</th>
                                    <th>Покупатель</th>
                                    <th>Фотоаппарат</th>
                                    <th>Сумма</th>
                                    <th>Статус</th>
                                    <th>Дата</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for purchase in recent_purchases %}
                                <tr>
                                    <td class="fw-bold text-primary">#{{ purchase.id }}</td>
                                    <td>
                                        <small>{{ purchase.customer.firstName }} {{ purchase.customer.lastName }}</small>
                                    </td>
                                    <td>
                                        <small>{{ purchase.camera.brand.name }} {{ purchase.camera.model }}</small>
                                    </td>
                                    <td>
                                        <strong class="text-success">{{ purchase.totalPrice|number_format(0, ',', ' ') }} ₽</strong>
                                    </td>
                                    <td>
                                        {% if purchase.status == 'завершен' %}
                                            <span class="badge bg-success">{{ purchase.status }}</span>
                                        {% elseif purchase.status == 'доставляется' %}
                                            <span class="badge bg-warning text-dark">{{ purchase.status }}</span>
                                        {% else %}
                                            <span class="badge bg-info text-dark">{{ purchase.status }}</span>
                                        {% endif %}
                                    </td>
                                    <td>
                                        <small class="text-muted">{{ purchase.purchaseDate|date('d.m.Y H:i') }}</small>
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <!-- Быстрые действия -->
        <div class="col-lg-4 mb-4">
            <div class="card shadow-sm">
                <div class="card-header bg-light">
                    <h5 class="mb-0">⚡ Быстрые действия</h5>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a href="{{ path('app_brand') }}" class="btn btn-primary btn-lg">
                            🅱️ Управление брендами
                        </a>
                        <a href="{{ path('app_camera') }}" class="btn btn-success btn-lg">
                            📸 Управление камерами
                        </a>
                        <a href="{{ path('app_customer') }}" class="btn btn-warning btn-lg">
                            👥 Управление покупателями
                        </a>
                        <a href="{{ path('app_purchase') }}" class="btn btn-info btn-lg">
                            🛒 Управление продажами
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}